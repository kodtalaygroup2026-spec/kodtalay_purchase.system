import { PageLoading } from "@/components/shared/PageLoading";

// แสดงระหว่างโหลดหน้าใน dashboard — sidebar/navbar คงอยู่ เปลี่ยนเฉพาะพื้นที่เนื้อหา
export default function Loading() {
  return <PageLoading />;
}
